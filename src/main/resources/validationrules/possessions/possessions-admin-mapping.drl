package validationrules.possessions;

import uk.gov.hmcts.reform.orgrolemapping.domain.model.CaseWorkerAccessProfile;
import uk.gov.hmcts.reform.orgrolemapping.domain.model.FeatureFlag;
import uk.gov.hmcts.reform.orgrolemapping.domain.model.RoleAssignment;
import uk.gov.hmcts.reform.orgrolemapping.domain.model.constants.RoleAssignmentConstants.Attributes;
import uk.gov.hmcts.reform.orgrolemapping.domain.model.constants.RoleAssignmentConstants.RoleName;
import uk.gov.hmcts.reform.orgrolemapping.domain.model.enums.ActorIdType;
import uk.gov.hmcts.reform.orgrolemapping.domain.model.enums.Classification;
import uk.gov.hmcts.reform.orgrolemapping.domain.model.enums.FeatureFlagEnum;
import uk.gov.hmcts.reform.orgrolemapping.domain.model.enums.GrantType;
import uk.gov.hmcts.reform.orgrolemapping.domain.model.enums.Jurisdiction;
import uk.gov.hmcts.reform.orgrolemapping.domain.model.enums.RoleCategory;
import uk.gov.hmcts.reform.orgrolemapping.domain.model.enums.RoleType;
import uk.gov.hmcts.reform.orgrolemapping.domain.model.enums.crd.JobTitle;
import uk.gov.hmcts.reform.orgrolemapping.util.JacksonUtils;
import com.fasterxml.jackson.databind.JsonNode;

import java.util.HashMap
import java.util.Map

import function uk.gov.hmcts.reform.orgrolemapping.domain.service.RequestMappingService.logMsg;


/*
 * 1: Possessions admin "hearing-centre-team-leader" role mapping.
 */
rule "possessions_hearing_centre_team_leader_role"
when
    $f:  FeatureFlag(status && flagName == FeatureFlagEnum.POSSESSIONS_WA_1_0.getValue())
    $cap: CaseWorkerAccessProfile(hasValidJobTitle(JobTitle.HEARING_CENTRE_TEAM_LEADER),
                                  hasValidServiceCode(Jurisdiction.POSSESSIONS),
                                  !suspended)
then
    Map<String,JsonNode> attribute = new HashMap<>();
    attribute.put(Attributes.Name.JURISDICTION, JacksonUtils.convertObjectIntoJsonNode(Jurisdiction.POSSESSIONS.getName()));
    attribute.put(Attributes.Name.PRIMARY_LOCATION, JacksonUtils.convertObjectIntoJsonNode($cap.getPrimaryLocationId()));
    attribute.put(Attributes.Name.REGION, JacksonUtils.convertObjectIntoJsonNode($cap.getRegionId()));
    attribute.put(Attributes.Name.WORK_TYPES, JacksonUtils.convertObjectIntoJsonNode("routine_work,applications,decision_making_work,error_management,appeals"));

    insert(
        RoleAssignment.builder()
            .actorIdType(ActorIdType.IDAM)
            .actorId($cap.getId())
            .roleCategory(RoleCategory.ADMIN)
            .roleType(RoleType.ORGANISATION)
            .roleName(RoleName.HEARING_CENTRE_TEAM_LEADER)
            .grantType(GrantType.STANDARD)
            .classification(Classification.PUBLIC)
            .readOnly(false)
            .attributes(attribute)
            .authorisations($cap.getSkillCodes())
            .build()
    );

    logMsg("Rule : possessions_hearing_centre_team_leader_role");
end;

/*
 * 2: Possessions admin "hearing-centre-admin" role mapping.
 */
rule "possessions_hearing_centre_admin_role"
when
    $f:  FeatureFlag(status && flagName == FeatureFlagEnum.POSSESSIONS_WA_1_0.getValue())
    $cap: CaseWorkerAccessProfile(hasValidJobTitle(JobTitle.HEARING_CENTRE_TEAM_LEADER,
                                                   JobTitle.HEARING_CENTRE_ADMIN),
                                  hasValidServiceCode(Jurisdiction.POSSESSIONS),
                                  !suspended)
then
    Map<String,JsonNode> attribute = new HashMap<>();
    attribute.put(Attributes.Name.JURISDICTION, JacksonUtils.convertObjectIntoJsonNode(Jurisdiction.POSSESSIONS.getName()));
    attribute.put(Attributes.Name.PRIMARY_LOCATION, JacksonUtils.convertObjectIntoJsonNode($cap.getPrimaryLocationId()));
    attribute.put(Attributes.Name.REGION, JacksonUtils.convertObjectIntoJsonNode($cap.getRegionId()));
    attribute.put(Attributes.Name.WORK_TYPES, JacksonUtils.convertObjectIntoJsonNode("routine_work,applications,decision_making_work,error_management,appeals"));

    insert(
        RoleAssignment.builder()
            .actorIdType(ActorIdType.IDAM)
            .actorId($cap.getId())
            .roleCategory(RoleCategory.ADMIN)
            .roleType(RoleType.ORGANISATION)
            .roleName(RoleName.HEARING_CENTRE_ADMIN)
            .grantType(GrantType.STANDARD)
            .classification(Classification.PUBLIC)
            .readOnly(false)
            .attributes(attribute)
            .authorisations($cap.getSkillCodes())
            .build()
    );

    logMsg("Rule : possessions_hearing_centre_admin_role");
end;

/*
 * 3: Possessions admin "hmcts-admin" role mapping.
 */
rule "possessions_hmcts_admin_role"
when
    $f:  FeatureFlag(status && flagName == FeatureFlagEnum.POSSESSIONS_WA_1_0.getValue())
    $cap: CaseWorkerAccessProfile(hasValidJobTitle(JobTitle.HEARING_CENTRE_TEAM_LEADER,
                                                   JobTitle.HEARING_CENTRE_ADMIN,
                                                   JobTitle.WLU_ADMIN,
                                                   JobTitle.WLU_TEAM_LEADER,
                                                   JobTitle.BAILIFF_ADMIN),
                                  hasValidServiceCode(Jurisdiction.POSSESSIONS),
                                  !suspended)
then
    Map<String,JsonNode> attribute = new HashMap<>();
    insert(
        RoleAssignment.builder()
            .actorIdType(ActorIdType.IDAM)
            .actorId($cap.getId())
            .roleCategory(RoleCategory.ADMIN)
            .roleType(RoleType.ORGANISATION)
            .roleName(RoleName.HMCTS_ADMIN)
            .grantType(GrantType.BASIC)
            .classification(Classification.PRIVATE)
            .readOnly(true)
            .attributes(attribute)
            .build()
    );

    logMsg("Rule : possessions_hmcts_admin_role");
end;

/*
 * 4: Possessions admin "task-supervisor" role mapping.
 */
rule "possessions_admin_task_supervisor_role"
when
    $f:  FeatureFlag(status && flagName == FeatureFlagEnum.POSSESSIONS_WA_1_0.getValue())
    $cap: CaseWorkerAccessProfile(hasValidJobTitle(JobTitle.HEARING_CENTRE_TEAM_LEADER,
                                                   JobTitle.HEARING_CENTRE_ADMIN,
                                                   JobTitle.WLU_ADMIN,
                                                   JobTitle.WLU_TEAM_LEADER,
                                                   JobTitle.BAILIFF_ADMIN),
                                  hasValidServiceCode(Jurisdiction.POSSESSIONS),
                                  isTaskSupervisor(),
                                  !suspended)
then
    Map<String,JsonNode> attribute = new HashMap<>();
    attribute.put(Attributes.Name.JURISDICTION, JacksonUtils.convertObjectIntoJsonNode(Jurisdiction.POSSESSIONS.getName()));
    attribute.put(Attributes.Name.PRIMARY_LOCATION, JacksonUtils.convertObjectIntoJsonNode($cap.getPrimaryLocationId()));
    if($cap.hasValidJobTitle(JobTitle.HEARING_CENTRE_TEAM_LEADER,
                             JobTitle.HEARING_CENTRE_ADMIN,
                             JobTitle.BAILIFF_ADMIN)) {
                attribute.put(Attributes.Name.REGION, JacksonUtils.convertObjectIntoJsonNode($cap.getRegionId()));
                }

    insert(
        RoleAssignment.builder()
            .actorIdType(ActorIdType.IDAM)
            .actorId($cap.getId())
            .roleCategory(RoleCategory.ADMIN)
            .roleType(RoleType.ORGANISATION)
            .roleName(RoleName.TASK_SUPERVISOR)
            .grantType(GrantType.STANDARD)
            .classification(Classification.PUBLIC)
            .readOnly(false)
            .attributes(attribute)
            .authorisations($cap.getSkillCodes())
            .build()
    );

    logMsg("Rule : possessions_admin_task_supervisor_role");
end;

/*
 * 5: Possessions admin "case-allocator" role mapping.
 */
rule "possessions_admin_case_allocator_role"
when
    $f:  FeatureFlag(status && flagName == FeatureFlagEnum.POSSESSIONS_WA_1_0.getValue())
    $cap: CaseWorkerAccessProfile(hasValidJobTitle(JobTitle.HEARING_CENTRE_TEAM_LEADER,
                                                   JobTitle.HEARING_CENTRE_ADMIN,
                                                   JobTitle.WLU_ADMIN,
                                                   JobTitle.WLU_TEAM_LEADER,
                                                   JobTitle.BAILIFF_ADMIN),
                                  hasValidServiceCode(Jurisdiction.POSSESSIONS),
                                  isCaseAllocator(),
                                  !suspended)
then
    Map<String,JsonNode> attribute = new HashMap<>();
    attribute.put(Attributes.Name.JURISDICTION, JacksonUtils.convertObjectIntoJsonNode(Jurisdiction.POSSESSIONS.getName()));
    attribute.put(Attributes.Name.PRIMARY_LOCATION, JacksonUtils.convertObjectIntoJsonNode($cap.getPrimaryLocationId()));
    if($cap.hasValidJobTitle(JobTitle.HEARING_CENTRE_TEAM_LEADER,
                             JobTitle.HEARING_CENTRE_ADMIN,
                             JobTitle.BAILIFF_ADMIN)) {
            attribute.put(Attributes.Name.REGION, JacksonUtils.convertObjectIntoJsonNode($cap.getRegionId()));
                }

    insert(
        RoleAssignment.builder()
            .actorIdType(ActorIdType.IDAM)
            .actorId($cap.getId())
            .roleCategory(RoleCategory.ADMIN)
            .roleType(RoleType.ORGANISATION)
            .roleName(RoleName.CASE_ALLOCATOR)
            .grantType(GrantType.STANDARD)
            .classification(Classification.PUBLIC)
            .readOnly(false)
            .attributes(attribute)
            .authorisations($cap.getSkillCodes())
            .build()
    );

    logMsg("Rule : possessions_admin_case_allocator_role");
end;

/*
 * 6: Possessions admin "wlu-admin" role mapping.
 */
rule "possessions_wlu_admin_role"
when
    $f:  FeatureFlag(status && flagName == FeatureFlagEnum.POSSESSIONS_WA_1_0.getValue())
    $cap: CaseWorkerAccessProfile(hasValidJobTitle(JobTitle.WLU_ADMIN,
                                                   JobTitle.WLU_TEAM_LEADER),
                                  hasValidServiceCode(Jurisdiction.POSSESSIONS),
                                  !suspended)
then
    Map<String,JsonNode> attribute = new HashMap<>();
    attribute.put(Attributes.Name.JURISDICTION, JacksonUtils.convertObjectIntoJsonNode(Jurisdiction.POSSESSIONS.getName()));
    attribute.put(Attributes.Name.PRIMARY_LOCATION, JacksonUtils.convertObjectIntoJsonNode($cap.getPrimaryLocationId()));
    attribute.put(Attributes.Name.WORK_TYPES, JacksonUtils.convertObjectIntoJsonNode("routine_work,applications,decision_making_work,error_management"));

    insert(
        RoleAssignment.builder()
            .actorIdType(ActorIdType.IDAM)
            .actorId($cap.getId())
            .roleCategory(RoleCategory.ADMIN)
            .roleType(RoleType.ORGANISATION)
            .roleName(RoleName.WLU_ADMIN)
            .grantType(GrantType.STANDARD)
            .classification(Classification.PUBLIC)
            .readOnly(false)
            .attributes(attribute)
            .authorisations($cap.getSkillCodes())
            .build()
    );

    logMsg("Rule : possessions_wlu_admin_role");
end;
/*
 * 7: Possessions admin "wlu-team-leader" role mapping.
 */
rule "possessions_wlu_team_leader_role"
when
    $f:  FeatureFlag(status && flagName == FeatureFlagEnum.POSSESSIONS_WA_1_0.getValue())
    $cap: CaseWorkerAccessProfile(hasValidJobTitle(JobTitle.WLU_TEAM_LEADER),
                                  hasValidServiceCode(Jurisdiction.POSSESSIONS),
                                  !suspended)
then
    Map<String,JsonNode> attribute = new HashMap<>();
    attribute.put(Attributes.Name.JURISDICTION, JacksonUtils.convertObjectIntoJsonNode(Jurisdiction.POSSESSIONS.getName()));
    attribute.put(Attributes.Name.PRIMARY_LOCATION, JacksonUtils.convertObjectIntoJsonNode($cap.getPrimaryLocationId()));
    attribute.put(Attributes.Name.WORK_TYPES, JacksonUtils.convertObjectIntoJsonNode("routine_work,applications,decision_making_work,error_management"));

    insert(
        RoleAssignment.builder()
            .actorIdType(ActorIdType.IDAM)
            .actorId($cap.getId())
            .roleCategory(RoleCategory.ADMIN)
            .roleType(RoleType.ORGANISATION)
            .roleName(RoleName.WLU_TEAM_LEADER)
            .grantType(GrantType.STANDARD)
            .classification(Classification.PUBLIC)
            .readOnly(false)
            .attributes(attribute)
            .authorisations($cap.getSkillCodes())
            .build()
    );

    logMsg("Rule : possessions_wlu_team_leader_role");
end;

/*
 * 8: Possessions admin "bailiff-admin" role mapping.
 */
rule "possessions_bailiff_admin_role"
when
    $f:  FeatureFlag(status && flagName == FeatureFlagEnum.POSSESSIONS_WA_1_0.getValue())
    $cap: CaseWorkerAccessProfile(hasValidJobTitle(JobTitle.BAILIFF_ADMIN),
                                  hasValidServiceCode(Jurisdiction.POSSESSIONS),
                                  !suspended)
then
    Map<String,JsonNode> attribute = new HashMap<>();
    attribute.put(Attributes.Name.JURISDICTION, JacksonUtils.convertObjectIntoJsonNode(Jurisdiction.POSSESSIONS.getName()));
    attribute.put(Attributes.Name.PRIMARY_LOCATION, JacksonUtils.convertObjectIntoJsonNode($cap.getPrimaryLocationId()));
    attribute.put(Attributes.Name.WORK_TYPES, JacksonUtils.convertObjectIntoJsonNode("enforcement_support,error_management"));

    insert(
        RoleAssignment.builder()
            .actorIdType(ActorIdType.IDAM)
            .actorId($cap.getId())
            .roleCategory(RoleCategory.ADMIN)
            .roleType(RoleType.ORGANISATION)
            .roleName(RoleName.BAILIFF_ADMIN)
            .grantType(GrantType.STANDARD)
            .classification(Classification.PUBLIC)
            .readOnly(false)
            .attributes(attribute)
            .authorisations($cap.getSkillCodes())
            .build()
    );

    logMsg("Rule : possessions_bailiff_admin_role");
end;

/*
 * 8: Possessions admin "specific-access-approver-admin" role mapping.
 */
rule "possessions_specific_access_approver_admin_role"
when
    $f:  FeatureFlag(status && flagName == FeatureFlagEnum.POSSESSIONS_WA_1_0.getValue())
    $cap: CaseWorkerAccessProfile(hasValidJobTitle(JobTitle.HEARING_CENTRE_TEAM_LEADER,
                                                   JobTitle.WLU_TEAM_LEADER),
                                  hasValidServiceCode(Jurisdiction.POSSESSIONS),
                                  !suspended)
then
    Map<String,JsonNode> attribute = new HashMap<>();
    attribute.put(Attributes.Name.JURISDICTION, JacksonUtils.convertObjectIntoJsonNode(Jurisdiction.POSSESSIONS.getName()));
    attribute.put(Attributes.Name.PRIMARY_LOCATION, JacksonUtils.convertObjectIntoJsonNode($cap.getPrimaryLocationId()));
    attribute.put(Attributes.Name.WORK_TYPES, JacksonUtils.convertObjectIntoJsonNode("access_requests"));
    if($cap.hasValidJobTitle(JobTitle.HEARING_CENTRE_TEAM_LEADER)) {
                        attribute.put(Attributes.Name.REGION, JacksonUtils.convertObjectIntoJsonNode($cap.getRegionId()));
                }

    insert(
        RoleAssignment.builder()
            .actorIdType(ActorIdType.IDAM)
            .actorId($cap.getId())
            .roleCategory(RoleCategory.ADMIN)
            .roleType(RoleType.ORGANISATION)
            .roleName(RoleName.SPECIFIC_ACCESS_APPROVER_ADMIN)
            .grantType(GrantType.STANDARD)
            .classification(Classification.PUBLIC)
            .readOnly(false)
            .attributes(attribute)
            .authorisations($cap.getSkillCodes())
            .build()
    );

    logMsg("Rule : possessions_specific_access_approver_admin_role");
end;

